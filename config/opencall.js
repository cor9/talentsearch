// Open Call display configuration.
// Update this file for each new Open Call — keeps year-specific copy in one place.
// Database-authoritative values (dates, event status, review window) come from
// the live p101_opencall_events row and must not be duplicated here.

export const OPEN_CALL = {
  edition: '11th',
  displayYear: 2026,
  eventTitle: 'Child Actor 101 Online Talent Representation Open Call',
  headshotCount: '100+',
}
